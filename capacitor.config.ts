import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.bookcollection',
  appName: 'Book Collection Manager',
  webDir: 'dist'
};

export default config;
